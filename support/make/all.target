#-*-Makefile-*- vim:syntax=make
#$Id$

PLATFORMS ?= mica2 telosb micaz

all: FORCE
	for target in $(filter-out $(INVALID_PLATFORMS),$(PLATFORMS)); do cmd="make $$target"; echo ".... $$cmd"; $$cmd; done

